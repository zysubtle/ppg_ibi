# 04_OPEN_QUESTIONS.md

版本：v0.1

本文件记录后续需要确认的问题。M1 阶段不要求 Owner 全部回答。

## M2 阻塞问题

这些问题需要在 M2 冻结，否则会影响 API 或核心算法策略。

| 编号 | 问题 | 当前默认假设 | 风险等级 |
|---|---|---|---|
| Q1 | 时间戳类型和单位是什么？ | `uint32_t timestamp_ms`，单位 ms | S0 |
| Q2 | 外部运动标志的字段名和语义是什么？ | `allow_measure == true` 表示允许测量 | S0 |
| Q3 | `allow_measure == false` 时如何处理内部历史？ | 进入 HOLD / REACQUIRE，停止输出 | S0 |
| Q4 | 丢点达到什么程度触发 REACQUIRE？ | 根据时间戳间隔阈值判断 | S0 |
| Q5 | IBI 输出单位是什么？ | ms | S0 |
| Q6 | 置信度范围是什么？ | 0.0 到 1.0 | S0 |
| Q7 | ECG 验证时 beat matching 规则是什么？ | 待定义 | S0 |

## M2 非阻塞但应记录的问题

| 编号 | 问题 | 当前默认假设 | 风险等级 |
|---|---|---|---|
| Q8 | PPG 原始 ADC 是否为 24-bit signed？ | `int32_t` 承载 signed 原始值 | S2 |
| Q9 | 是否需要输出主通道、SQI、峰值位置等调试信息？ | 需要可选 debug 输出 | S2 |
| Q10 | 是否需要支持 timestamp wraparound？ | M2 再决定 | S2 |
| Q11 | 是否需要 host 端 CSV 验证工具？ | 需要 | S2 |
| Q12 | 是否需要 MCU 编译工程？ | 暂不需要，先做纯算法库 | S2 |

## 后续验证阶段问题

1. ECG 真值的数据格式是什么？
2. PPG 和 ECG 是否共用时间戳？
3. 是否存在 PPG / ECG 时间偏移？
4. 是否有不同运动/静息场景标签？
5. 验收阈值如何定义：MAE、P95、漏检率、误报率、覆盖率？
